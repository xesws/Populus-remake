# 开发顺序（用户锁定）

参照《开天辟地》。Object 过了。按 1→2→3，不许跳。

## 已过：Object

高度场、贴地、斜房子。七个 kind。稿 `OBJECT.md` + `CLASSIC.md`。

## 已过：生产 + 鼠标交互

稿：`PRODUCTION.md` + `INTERACT.md`。
默认左键选人，建造走 3D 预览，征召/谕令打在选中的人上。验收过了。

## 已过：训练排队

稿：`TRAIN.md`。贴墙绕圈、同时只训 1 人、己方营顶进度。验收过了。

## 已过：魔法 · 沼泽

稿：`MAGIC.md`。枯枝 + 低雾，人堵住，停久毒死。

## 已过：魔法 · 火山

稿：`MAGIC.md`。慢抬小高台，岩浆外流飞溅，外沿焦土。

## 已过：魔法 · 地震

稿：`MAGIC.md`。地裂开成沟，人滑进去，裂到的房子塌（屋宇 2→1）。

## 已过：房子骨架血量

稿：`MAGIC.md`。完好 → 柱梁骨架（屋宇还算）→ 再烧没（屋宇 -1）。岩浆测过了。

## 已过：魔法 · 龙卷风

稿：`MAGIC.md`。灰白漏斗贴地乱走，卷人吸起，卷房变骨架。

## 已过：魔法 · 闪电

稿：`MAGIC.md`。看得见的折线劈地，人着火并打飞，劈房变骨架。

## 已过：魔法 · 击飞

稿：`MAGIC.md`。人打飞离地，不着火，旁边房子完好（屋宇不变）。

## 已过：魔法清单

沼泽 / 火山 / 地震 / 骨架 / 龙卷风 / 闪电 / 击飞。冻着，先不回头改。

## 已过：人物房屋 · 选中点地走

稿：`HOUSE.md`。选中点地走，右键取消环仍走到点，选中的勇士才去盖。

## 已过：人物房屋 · 寻路不卡

稿：`HOUSE.md`。绕房绕树，盖房站垫外沿，卡住约 1 秒再绕。

## 已过：人物房屋 · 住人升屋

稿：`HOUSE.md`。空屋不产；选中勇士入住才产；产到数量升 L2；屋顶有产速。没跳 L3，没堆木头。

## 已过：等 Jerry 下一刀

人物房屋三刀收了。Jerry 已开口。

## 已过：右键走 + 开战

稿：`COMBAT.md` + `INTERACT.md`。右键点地走（前往标记），左键空地取消；对准敌人出 3D 拳头，右键打人 / 拆屋。魔法冻着。不要火球、传教、间谍放火。

## 已过：大龙（飞龙）

稿：`DRAGON.md`。大龙训练营（工厂外观、6 木）→ 20 牛战士进驻满 → 60s 生产 → 大龙空降。600 血、10 格射程、吐息火 patch 衰减燃烧；魔法必中（只掉血）、牛战士/哨塔对空、近战够不着；剪影光标拾取修复半空误选。

## 已过：敌方 AI 修复与增强（v0.31）

调研定位四类根因并修复：① 建营者被入住/批量训兵吞掉、`occupy` 不清 `foundKind` 永久占死名额——火战士/间谍线 0 产出的根因；② 训兵"全量入队"冲垮 `armyCap` 与兵种配比，特种兵档位被满员冻结；③ 营地选址固定朝敌方前线（反复被拆），现改自家后方、哨塔朝敌，且选址/落基按真实占地判定（营地 2.6 ≠ 茅屋 1.3）；④ 受袭上报只有火球一处触发（近战/法术装死），现 `applyUnitDamage`/`applyBuildingDamage`/面积杀伤全覆盖 + 1s 节流。新增：AI 自主建哨塔（towerCap/towerGapSec 入 AIProfile）并自动派牛战士驻塔。测试 `ai-founder-check.ts` + `ai-defense-check.ts`。

## 已过：敌方 AI 军事升级（v0.37）

用户口径："造兵特别不积极，从来不知道主动把大量村民转化为更多武士去发动进攻；通常只有不到 10 个武士，也不知道去出别的兵种（比如火武士），更不知道拿火武士去训练大龙；进攻方式特别单一，就是派一两个、三个武士过来骚扰一下，然后被打爆。"

四类根因与修复：

① **编制口径**：旧 `armyCap=8` + 全局单条 `trainCd` + 阶梯只点 1 名牛战士——常备军封顶 8 人、同一时刻只有一座营能出人。现改为 `ArmyPolicy` 纯策略：常备军 = clamp(人口 × armyRatio, armyFloor, armyMax)（normal 24 / hard 36），牛战士占比 `fireRatio`，四条产线（武士营/牛战士营/神庙/间谍营）各自排队（queueDepth）与各自节流。

② **军令/民令分离**：旧 `launchWave/recall` 走 `setOrder(team,"fight"/"settle")`——进攻期间全队（含村民）的 `foundKind/targetId/settleX` 被清空（红方砍树/搬运/落基集体停摆），而 `finishTrain` 给新兵硬编码 `order="fight"`，每个新训成的武士各自冲向最近敌人逐个送命。新增 `Sim.setIdleArmyStance`（只改空闲士兵的 order，不碰 `teams[].order` 与村民）+ `hold/marshal` 姿态锚点。

③ **集团波次与战损加码**：门槛 `waveForce`（normal 8）攒够才出发，在自家前沿集结点成型后整队压上；`Targeting.assaultFocus` 让全波**集火同一焦点目标**（敌方建筑密集点、茅屋优先），`commandWave` 焦点被拆后续压下一点；惨败（战损 ≥ waveRetreatRatio）后门槛 +waveForceStep（封顶 waveForceMax），不再重复"1~3 人送死"。受袭只从留守池抽 defenseSize 人，波次在外时老家告急则整波回防。

④ **大龙计划**：人口达 `dragonPopMin` 后的大龙训练营当作第五座营地走既有建营链路；`DragonDirector` 逐批征召空闲牛战士进厂（20 名满员开工 → 60s → 大龙空降），出厂后按 `dragonOrderSec` 节拍压向敌方密集点，`dragonCrossSea` 决定分岛图是否跨海。

顺带修掉两处探针实测死锁：**编制硬顶把厂家牛战士算成野战军**（名额收窄后反而卡死训练，工厂停在 12/20 出不了龙）；**"空闲村民 − batch ≥ laborFloor"把全队都在干活误判成无人可征**——保底改看户外村民总数，人口到顶时从茅屋动员住户入伍（战争经济）。

观测量化（`npx tsx scripts/probe-ai-army.ts normal 8`，seed=42，蓝方木头玩家）：首波 123s/8 人，波次出击 8→25 人，常备军峰值 43，大龙训练营 104s、大龙 351s；easy 4~5 人/波且不追龙，hard 10→34 人/波、304s 出龙。测试 `ai-army-check.ts` + `ai-dragon-check.ts`。

## 当前：敌方 AI 群众路线（v0.38）

用户看完实战日志后拍板："军事人口上限改成 100 不就好了？"——并指出大龙被集火的问题。日志分析（seed=2208/2496）确认：红方 200 人口只养 24 个兵（95% 人口在茅屋里待着），而大龙每死一条就重新开龙厂又吞 20 名牛战士。

调整：

① **编制**：`armyMax` normal 24→**100**（= 红方 200 人口的一半）/ hard 36→140 / easy 8→30；`armyRatio` 0.3→0.5；`waveForceMax` 20→40（hard 60）。旧上限下 200 人口的 AI 只能拿出 24 个兵，现在按人口滚动而不是靠一个拍脑袋的小数字。

② **训练吞吐**：`trainGapSec` 8→**5**（红方单营训一人需 5.33s，8s 节流等于每轮空转 2.7s）——两座营从 9 人/分钟提到 ~22 人/分钟，不然 100 人的编制根本填不满。

③ **大龙不再自杀、也不再刷**：`dragonCap` 改口径为**每局总条数**（旧口径是场均存活数，龙一被集火秒掉就重启计划；实测一局 9 分钟被喂掉 40 名牛战士）；出动目标由"敌方最密集点"改为 `Targeting.softFocus`——挑**附近敌方军事单位/塔最少**的建筑（优先外围茅屋）；血量低于 `dragonRetreatHp`（35%）召回自家蓊落。

④ **经济劳力保底**：人口到顶时 `assignHomes` 不再把户外村民全塞进屋（旧实测量到"户外 0 人"——没人砍木/搬运，营地被拆了也重建不起来）。

⑤ **顺手修的隔海漏**（实测发现）：旧 `seaProbe` 把目标点 `nearestLand` 吸附后再核对终点，目标贴海岸时会吸附到**己方那一侧**的岸→判可达→发波；而 `sendMove` 找不到路径时会用 `path=[{dest}]` 直线回退（忽略地形），于是红方士兵真的**徒步渡海**爬上蓝岛（seed=1 实测 9 名武士+2 名牛战士站在蓝岛）。现在先比岛屿标签（O(1)）再跑 astar。

⑥ **建房链路修复**（用户："我这边建造房屋的时候，老是会有几个房屋死活建不起来"）——四处断链：
   - **木料被别的工地抢走**（根因）：`repathSettle` 的扛木分支一律改投 `nearestNeedSite`，只要附近另有缺木工地，建工肩上的木头就被近处工地吸走——远处工地永远停在半成品。顺带实测：AI 开局有 1/6 概率首个兵营 0/4 木头、整局训不出兵（这就是玩家与 AI 共同的"房子建不起来"）。现改为 `woodDropSite`：优先交给自己挂的工地（targetId / buildId），再退到最近。
   - **建房时选中里不是村民**：`assignBuilders` 静默无动作 → 工地永远没人。现在返回实际指派人数，game.ts 提示"先选中村民"，并由**工地看门狗**（无建工时自动召 2 名空闲村民；只剩 1 名劳力时不抽、不抢玩家在用的建工）接上。
   - **住户/塔上单位当建工**：`homeId>0` 的单位被 thinkUnits 整体跳过，永远走不到工地——`assignBuilders` 现在先 `leaveBuilding` 叫出来（与 v0.27h 右键点地同语义）。
   - **右键走位让建工集体辞职**：`clearOrders` 会连 `buildId` 一起清，任何一次右键移动/聚集都让工地失去全部建工。`buildId` 改为粘性（工地完工/被拆时自动卸任）。

测试：新增 `build-site-check.ts` 7 例（无建工不再僵尸/住户被叫出/走位不辞职/战死自动补人/保底不抢劳力/完工自动卸任/木料不被抢）。

观测量化（`npx tsx scripts/probe-ai-army.ts [档位] [分钟] [seed]`）：
- normal seed=42 8min：常备军峰值 **97**，波次出击 **8 → 94 人**，一座龙厂 87s、大龙 286s（仅 1 条）；
- 开局成军稳定性：150s 内训出 ≥4 兵 8/8 次（修木料被抢前 6 次里有 1 次整局卡在 0 兵）；
- hard seed=42 6min：峰值 76，波次 12 → 66 人；
- 分岛图 seed=1：波次 0（陆军不再渡海）+ 1 条龙打撑外婆（打掉玩家 2 座茅屋）。

测试：`ai-army-check.ts` 9 例（新增人口到顶时劳力保底）、`ai-dragon-check.ts` 5 例（新增软目标避主力、龙账本不重启、低血撤离）；`dragon-check.ts` 脱锁用例落点 12→16 格（消除龙盘旋进锁敌圈的假失败）。**分岛图仍需红方海军才能真正陆战对抗——v0.39 立项；在此之前可用 `?ai=hard` 打大陆图。**

## 不要做

- 没放行就开下一档。
- 征召即时变身。
- 把人再放大。
- 上贴图。
